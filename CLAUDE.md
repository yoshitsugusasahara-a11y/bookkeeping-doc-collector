# bookkeeping-doc-collector

会計事務所向けの証憑回収アプリ。顧客がレシートを送信すると、Gemini OCRで読み取り、マネーフォワード クラウド会計へ仕訳を登録する。

## サービスの位置づけ（設計判断の前提）

**このサービスは「AIが記帳を代行する」のではなく「AIが記帳をアシストする」もの。** AIの判定は参考情報であり、最終的な勘定科目の判断は利用者の責任とする。この前提が、いくつかの設計に直接現れている。

- 自動送信は**既定でオフ**。有効化は「内容を確認しないまま送られてよい」という同意にあたるため、顧客本人しか設定できず、同意者と日時を記録する
- 予測仕訳を送信前に画面へ表示する。**中身が見えないまま押す確認ボタンは確認として機能しない**
- 税区分の正しさ（税務判断）は保証しない旨をユーザー向け文言で明示している

機能を追加・変更する際は、この前提を崩していないか確認すること。

## 技術スタック

Next.js 15 (App Router) / Supabase (DB・認証・Storage) / Vercel (ホスティング・Cron) / Gemini API (OCR・分類・仕訳生成) / Money Forward クラウド会計 API v3 / Google Drive API

## 開発の進め方

**実装に着手してよいのは、ユーザーが明示的に指示または承諾したときだけ。** 「どう思う？」「意見をください」といった相談は実装の許可ではない。設計案・トレードオフを提示し、承諾を待つ。

- コミット・プッシュはClaudeが直接gitコマンドで行う（実装が承諾された案件について）
- **SQLはClaudeが実行しない。** 内容を提示し、ユーザーがSupabaseのSQL Editorで実行する。コード側は `supabase/schema.sql` の更新に留める
- 通常の修正は `main` へ直接コミット。ブランチ運用はユーザーが明示したときのみ
- やり取りはすべて日本語（ツール呼び出し前後の進捗コメントも含む）

## 踏みやすい落とし穴

### OCRは一度完了すると再実行されない

`ocr_status` が `completed` の資料は、保存済みの値を返してGeminiを呼ばない。**OCRプロンプトや顧客別の仕訳生成指示を変更しても、既存の資料には反映されない。** 検証時は必ず「読み取り直す」ボタンを使うか新規アップロードすること。これを忘れると「修正が効いていない」と誤診する。

### 予測仕訳は生成時のまま保存し、送信時に作り直さない

画面に表示した仕訳がそのまま送られることを保証するため（Geminiは温度を下げても完全には決定的でなく、モデルのフォールバックもある）。OCR結果を編集すると破棄され、作り直される。

### customer_accounts は顧客セッションから更新できない

RLSポリシー上、更新は管理者のみ。顧客セッションからのUPDATEは**0件更新になり、エラーも出ずに黙って失敗する**。顧客自身の設定変更は、所有者を確認した上で管理用クライアント（`createAdminClient()`）を使い、更新行数を検証すること。同じ理由で `receipt_uploads` の削除も顧客セッションでは失敗する。

### Money Forward API の細かな仕様

- 税区分の税率フィールドは `tax_rate`（小数。`0.08`）であって `rate` ではない。軽減税率の名称は「(軽)」表記で「軽減」の文字列は含まれない
- 軽減税率を探すときは、対象科目のマスタ既定税区分から**系統**（課税仕入／共通課税仕入／非課税対応仕入など）を特定し、同じ系統の8%版を選ぶ。グローバルに「8%を探す」と別系統を拾う
- 事業者情報のパスは複数形の `/api/v3/offices`（単数形は存在しない）
- **免税事業者では `/api/v3/taxes?available=true` が空配列を返す。** これを課税/免税の自動判定に使っている
- **勘定科目マスタの `tax_id` は常に信頼して採用する。** 誤りがあっても指摘しない（税理士業務にあたるため）

### Next.js / Vercel

- `serverActions.bodySizeLimit` は Next.js 15 でも `experimental` 配下。トップレベルに置くと無視され既定の1MBに戻る
- Vercelのリクエストボディ上限4.5MBは設定で引き上げられない。上限を超える画像はブラウザ側で縮小してから送っている（`lib/images/shrink-image.ts`）
- バックグラウンド処理（`after()`）にも実行時間の上限がかかる。1リクエストで大量のOCRを処理させないこと

### Geminiは存在しない勘定科目IDを作ることがある

プロンプトで禁止しても起きる。生成時にマスタと突き合わせ、不正なら試行を失敗扱いにしてリトライする。

## 主要なディレクトリ

```
app/
  admin/customers/[customerId]/   管理者の顧客詳細（設定・履歴・送信操作）
  client/[clientSlug]/            顧客画面（送信・履歴・設定）
  api/cron/process-receipts/      日次処理
lib/
  gemini/       OCR・資料分類・仕訳生成
  moneyforward/ API呼び出し・予測仕訳・事業者情報・トークン管理
  receipts/     処理フロー・送信モード・保存上限
  images/       アップロード前の画像縮小
supabase/schema.sql   スキーマ（手動実行したSQLをここに反映する）
```

## 補足

- 開発の優先順位は課題管理表（Google Sheets）が正典。Claudeには書き込み権限がないため、ステータス更新はユーザーが行う
- 顧客名などの個別情報や、過去の設計判断の経緯はローカルの記憶ファイルに記録している（このファイルには含めない）
